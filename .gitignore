# Secrets — never commit
.env
.env.*
!.env.example

# deps / build
node_modules/
.next/
out/
build/
coverage/
__pycache__/
*.pyc
.venv/

# runtime logs
*.log
logs/

# Private project notes — never commit or push
PROJECT-NOTES.md

# Real-secret variant of the Zerops import — paste into dashboard only
zerops-project-import.local.yaml
